# Directories below will not be scanned at all.
# Paths are relative to the root of the project.
exclude_paths:
  - 'db/migrate'  # Ignore all Rails migration files

# Auto generated by Reeks --todo flag
detectors:
  IrresponsibleModule:
    exclude:
    - UsageMeter::ApplicationController
    - UsageMeter::ApplicationHelper
    - UsageMeter::ApplicationJob
    - UsageMeter::ApplicationMailer
    - UsageMeter::ApplicationRecord
    - ApplicationController
    - ApplicationHelper
    - ApplicationJob
    - ApplicationMailer
    - ApplicationRecord
    - Dummy::Application
    - NavigationTest
    - UsageMeter::CustomerTest
    - UsageMeterTest
    - UsageMeter::Engine
    - UsageMeter
